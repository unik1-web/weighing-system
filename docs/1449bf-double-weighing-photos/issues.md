# Реестр проблем: 1449bf-double-weighing-photos

## Итог

Задача завершена. Зафиксировано проблем: 0 (возвратов между этапами не было).

Задача прошла все этапы без возвратов.

## Проблемы в процессе разработки

Нет. Executions 1→2→3→4→5→6 шли последовательно без `fail` и без возвратов analysis/architect/development.

## Замечания код-ревью

Некритичные (execution 4, Approve — на доработку не возвращали):

1. **Размер файла** — `TicketPhotoPreview.tsx` ~477 строк; архитектор допускал вынос `PhotoLightbox` при >~350 — не блокер.
2. **Название теста порядка групп** — формулировка «both null → Тара then Брутто» слегка расходится с secondary sort по `created_at`; поведение и equal-case корректны.
3. **Опционально позже** — `createPortal` для lightbox; сейчас `fixed` + `z-[60]` достаточно относительно journal modal `z-50`.

## Проблемы тестирования

Нет упавших тестов и возвратов testing → development.

- Vitest: 153 passed, 0 failed
- Typecheck: ok
- FR-3 (lightbox): автотеста на диске нет; тестировщик предложил RTL-кейсы текстом (опционально, не блокер merge). Покрытие подтверждено статическим разбором (`PhotoLightbox`, клик только при `status===ok`, Esc / backdrop / «Закрыть»).
