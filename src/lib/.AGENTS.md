# Каталог: src/lib/

## Назначение
Доменная и инфраструктурная логика frontend: storage, runtime-клиенты, API-обёртки и вспомогательные правила.

## Файлы

### scale-runtime-client.ts
- Класс `ScaleRuntimeClient`:
  - `connect(activeScale)` — выбирает `web_serial` или `backend_api`;
  - `read(timeoutMs)` — читает через `ScaleConnection` (Web Serial) или `/api/scales/read`;
  - `getStatus()` — текущее состояние runtime;
  - `disconnect()` — завершает runtime-сессию;
  - нормализует runtime-ошибки в коды `invalid_connection_config`, `unsupported_transport`, `transport_unavailable`, `read_timeout`, `stale_session`, `backend_unavailable`;
  - выполняет не более одной автоматической retry-попытки, затем переводит форму в `manual_only`;
  - обработка `SITE_RUNTIME_CHANGED_EVENT` — закрывает backend session и переводит state в `disconnected`.
  - structured logging событий `connect/read/manual_only/retry` через `logger.scaleRuntime`.

### logger.ts
- Frontend logger:
  - хранит structured `runtime_context` (`site_id`, `scale_id`, `scale_role`, `adapter_id`, `transport`, `session_id`, `code`, `phase`);
  - применяет redaction для `COM/TTY/IP` в details/context;
  - подавляет повтор одинаковых runtime errors (debounce) и пишет агрегированный счётчик.

### site.ts
- Доменные функции площадки/комплектов:
  - `ensureDefaultSiteAndScales(settings)` — idempotent-инициализация и legacy-миграция `scale_device_id` в `scales.connection`;
  - `updateScaleConfiguration(scaleId, input, syncMirror)` — сохранение `adapter_id` и `connection`;
  - `ticketScaleFieldsFromRuntime()` — snapshot полей для новых талонов.

### storage.ts
- Локальные фасады хранения:
  - `SettingsStorage`, `ScaleStorage`, `SiteRuntimeStorage`, `TicketStorage`.
  - Нормализация legacy-конфигов: `adapter_id='web_serial'` + `connection.device_id` → канонический built-in `adapter_id`.

### weighing-mode.ts
- Pure-хелперы режима взвешивания:
  - `isManualWeightUsedOnCurrentStep(...)` — определяет, есть ли ручной источник среди весов текущего шага;
  - `isManualWeightReasonRequiredOnCurrentStep(...)` — проверяет обязательность причины по policy и текущим слотам.

### __tests__/scale-adapters-builtins.test.ts
- Unit/E2E тесты built-in parser-ов:
  - чтение общего fixture-pack
  - проверка parser-функций и registry-пути

### __tests__/generic-regex-contract.test.ts
- Unit/E2E тесты `generic-regex`:
  - preview-сценарии `preview_validated` и `pending_runtime`
  - отклонение non-portable regex
  - проверка лимитов `pattern`/`test_frame` и runtime frame `>1024`

### __tests__/site-runtime-switch.test.ts
- Unit/E2E тесты переключения active комплекта:
  - `applyScaleSetSwitch()` обновляет `site_runtime` и журнал;
  - no-op переключение не создаёт запись;
  - `alignDeviceMirror()` сохраняет последнее валидное mirror-значение;
  - `ScaleRuntimeClient` закрывает backend session по `SITE_RUNTIME_CHANGED_EVENT`.

### __tests__/scale-runtime-client.test.ts
- Unit-тесты runtime client:
  - выбор веток `web_serial` / `backend_api`;
  - fallback в `manual_only` для `unsupported_transport`, `read_timeout`, `transport_unavailable`, `stale_session`;
  - чтение Web Serial через listener `ScaleConnection`.

### __tests__/scale-adapters-parity.test.ts
- Frontend parity tests по shared fixture-pack:
  - built-in adapters (`microsim-m0601`, `newton`, `cas`, `midl-mi-vda`);
  - `generic-regex` runtime fixtures.

### __tests__/stable-mode-matrix.test.ts
- Тесты матрицы `stable_mode`:
  - четыре комбинации `stable_mode true/false` × `stable true/false`;
  - проверка разрешения/блокировки фиксации.

### __tests__/manual-weight-reason.test.ts
- Тесты обязательности причины ручного ввода:
  - single, dual first pass и dual completion;
  - различие policy `optional` / `required`;
  - проверка, что учитываются только слоты текущего шага.

### __tests__/logger.test.ts
- Unit-тесты observability logger:
  - debounce подавляет повтор одинаковой runtime-ошибки;
  - redaction скрывает `COM/TTY/IP` в сохранённых логах.

## Зависимости
- `src/lib/scale-adapters/` — каталог адаптеров и transport-типов.
- `server/scale_api.py` — backend stub contract `/api/scales/*`.
