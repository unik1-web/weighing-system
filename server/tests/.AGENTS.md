# Каталог: server/tests/

## Назначение
Pytest-набор backend: schema, API-контракты, runtime и регрессионные проверки.

## Файлы

### test_scale_api_stub_smoke.py
- Интеграционные тесты runtime API весов:
  - `connect -> status -> read -> disconnect`
  - `inactive_scale_mismatch` на `connect`
  - `unsupported_transport` для `tcp_client`
  - проверка единого error body (`code`, `message`)

### test_scale_runtime_switch.py
- Интеграционные тесты переключения active комплекта:
  - stale-session после switch runtime;
  - `status/read/disconnect` возвращают `409 stale_session`;
  - unrelated `/api/database` update не инвалидирует session.

### conftest.py
- Общие фикстуры:
  - изоляция `config.ini` и SQLite в temp-каталоге;
  - Flask test-client;
  - reset runtime session registry перед каждым тестом API;
  - подмена transport factory для deterministic serial runtime tests;
  - `temp_app_root` — изолированный app root (`BD/` + `Photo/`);
  - `temp_stage6_root` — изолированный app root для stage 6;
  - autouse-очистка `year_rotation` test hooks между тестами;
  - реэкспорт stage-6 helpers: `build_stage6_legacy_db`, `build_stage6_active_year_db`, `build_stage6_archive_db`, `freeze_stage6_now`, `corrupt_tmp_database`, `age_stage6_lock`.

### stage6_fixtures.py
- Программные builders временных SQLite-фикстур stage 6 (через `sqlite_store.init_schema`):
  - mixed legacy / empty legacy;
  - rotation-source (open tickets, pending REO, whitelist, optional extra `app_*`);
  - archive tickets (sent REO, mixed-legacy dates);
  - freeze now / corrupt tmp / age lock helpers.

### test_scale_api_guard.py
- Security tests runtime API:
  - `origin_not_allowed` для неразрешённого origin;
  - `auth_required` без active operator-session.

### test_scale_api_contract.py
- Контрактные тесты `/api/scales/*`:
  - success flow `connect -> status -> read -> disconnect`;
  - error-контракты `inactive_scale_mismatch`, `stale_session`, `invalid_connection_config`, `unsupported_transport`, `transport_unavailable`, `read_timeout`;
  - проверка стандартного error payload (`success/code/message`).

### test_scale_api_security.py
- Security guard `/api/scales/*`:
  - отказ для неразрешённого `Origin`;
  - `auth_required` без active operator-session;
  - `insufficient_permissions` при недостаточной роли;
  - отсутствие wildcard CORS (`Access-Control-Allow-Origin: *`).

### test_scale_runtime_registry.py
- Lifecycle тесты runtime session registry:
  - идемпотентный `connect`;
  - stale после switch/config write;
  - идемпотентный `disconnect`;
  - purge истёкших stale-marker;
  - `session_registry_overloaded` при переполнении.

### test_scale_adapter_parity.py
- Backend parity tests по shared fixture-pack:
  - built-in adapters;
  - `generic-regex` runtime fixtures.

### test_scale_integrity.py
- Unit-тесты integrity-аудита и redacted logging:
  - различение `fatal` и `warning` findings;
  - скрытие приватных `COM/TTY/IP` в structured runtime logs.

### test_serial_backend_transport.py
- Unit tests `SerialBackendTransport`:
  - timeout mapping (`read_timeout`);
  - open/read transport failures (`transport_unavailable`).

### test_stage5_migration.py
- Unit/E2E tests migration runbook stage 5:
  - backup/import round-trip для `manual_weight_reason_policy`, `manual_weight_reason`, `connection_json`;
  - идемпотентный повторный запуск `migrate_legacy_storage()` с `PRAGMA user_version = 5`;
  - проверка создания backup-пары `config.ini` + `BD/weighing.db`.

### test_smoke_yearly_archive_evidence.py
- Unit-тесты evidence renderer `scripts/smoke_yearly_archive.py`:
  - markdown/json со статусами PASS/FAIL по шагам;
  - регистрация CLI scenarios `active` / `archive` / `fail-retry` / `parallel-lock`.

### test_stage6_stub_skeleton.py
- Unit/E2E tests каркаса stage 6:
  - `get_sqlite_path(year, suffix)` для legacy/year/tmp путей;
  - `read_active_year()`/`write_active_year()` через `config.ini[settings]`;
  - add-only `migrate_schema_stage_6()` (колонки, индексы, `user_version = 6`);
  - smoke-контракты новых endpoint-ов `/api/archive/*` и `/api/year/rotation/*`;
  - проверка совместимости `/api/config` и `/api/database` после подключения stage-6 каркаса.

### test_stage6_stub_contract.py
- Контрактные проверки stage-6 archive/rotation:
  - обязательные поля в ответах `/api/archive/*` и `/api/year/rotation/*`;
  - валидация кода `invalid_archive_year` при отсутствии параметра `year`;
  - no-mock запуск `scripts/smoke_yearly_archive.py` против реального HTTP entrypoint `python server/app.py`.

### test_archive_read_flow.py
- Тесты archive-read (каталог лет, журнал, карточка, reprint isolation):
  - `list_archive_years` по именам файлов (исключает active year / `.tmp` / invalid);
  - `validate_archive_year` только для ГГГГ;
  - `build_mixed_legacy_warning` при расхождении даты тикета и года файла;
  - E2E: список 20 архивов, открытие выбранного года без изменения active DB;
  - E2E: mixed legacy warning сохраняет выбранный год;
  - E2E: чтение completed ticket не копирует запись в active year;
  - E2E: повреждённый архив → `archive_open_failed`, другой год остаётся доступен.

### test_archive_edit_flow.py
- Тесты admin-правки архива и diff-аудита:
  - unit: `validate_archive_patch`, `recalculate_archive_ticket`, `build_archive_edit_audit_event`;
  - E2E: admin PATCH сохраняет allowlist-поля и пишет `ticket_audit.archive_edit`;
  - E2E: non-admin → `403 insufficient_permissions`;
  - E2E: `reo_status=sent` без ack → `409 archive_reo_ack_required`, с ack → warning и статус `sent`;
  - E2E: forbidden fields / no-op diff без записи аудита;
  - smoke: active journal не меняется после archive edit.

### test_stage6_primary_migration.py
- Тесты задачи 2.1 (первичная миграция legacy -> yearly):
  - `resolve_migration_year()` по данным тикетов и для пустой БД;
  - copy-on-write операции `create_database_backup` / `create_tmp_copy_from_legacy` / `publish_tmp_database`;
  - backfill новых stage-6 колонок `ticket_audit`;
  - E2E-сценарии mixed legacy, empty legacy, target conflict, resume после сбоя между publish и `write_active_year`.

### test_stage6_rotation_flow.py
- Тесты задачи 2.2 (preview/commit ротации):
  - preview для `open`-тикетов (dictionary tare + `tara_default`) без записи в БД;
  - guard `assert_active_db_write_allowed` для fresh/stale lock и `allow_rotation_commit=True`;
  - whitelist-copy и deny-by-default проверка runtime/session данных target DB;
  - успешный commit: backup, auto-close + `ticket_audit`, publish нового года, очистка сессии;
  - блокировка параллельного commit (`rotation_in_progress`) и stale-lock recovery (`resume_tmp`).

### test_active_year_service.py
- Тесты active-year CRUD слоя:
  - `next_ticket_number()` для пустой/заполненной годовой БД;
  - `write_active_storage()` с write-gate (`rotation_in_progress`);
  - диагностика `get_active_ticket_numbering_state()`.

### test_stage6_logging.py
- Structured logging stage 6:
  - unit: `redact_sensitive_stage6_context` маскирует `reo_access_key` / `*_db_password` / connection;
  - unit: `log_stage6_event` всегда пишет `event`/`status` и применимые поля;
  - E2E: primary migration start/итог/backup;
  - E2E: rotation commit + fail `rotation_in_progress` + stale-recovery;
  - E2E/unit: archive-edit success/forbidden/noop/warning без полного diff; diff только в `ticket_audit`.

### test_stage6_migration.py
- Backend HTTP-интеграция UC-01 / TF-01:
  - mixed legacy через `GET /api/config` bootstrap → один yearly-файл + warning;
  - empty legacy → текущий Gregorian год;
  - конфликт существующего target → `migration_target_exists` без порчи legacy.

### test_stage6_rotation.py
- Backend HTTP-интеграция UC-02 / TF-02..TF-05:
  - preview/commit через `/api/year/rotation/*`;
  - fail/retry после backup и после `.tmp` (явные hooks, без двойного auto-close);
  - parallel commit → `409 rotation_in_progress`;
  - deny-by-default лишних `app_*` блокирует publish;
  - stale `preview_token` → `409 rotation_preview_stale`.

### test_stage6_archive_api.py
- Archive read contracts:
  - error matrix: `invalid_archive_year`, `archive_year_not_found`, `archive_open_failed`;
  - mixed legacy warning при чтении журнала/карточки.

### test_stage6_archive_edit.py
- Archive edit contracts:
  - TF-03 sent REO + ack / `reo_divergence_warning`;
  - forbidden-field matrix (каждый denylist-ключ по одному → `archive_edit_forbidden_field`);
  - allowlist happy-path с пересчётом и аудитом.

### test_scale_release_gate_artifacts.py
- Unit-тесты артефактов CI/release gate:
  - наличие строк `EC-01..EC-09` со статусом `PASS/FAIL` в acceptance-report;
  - наличие wrapper-команд `test:scale-*` в `package.json` и `test-scale/smoke-scale/release-check-scale` в `Makefile`;
  - наличие обязательных job/evidence-проверок в workflow `scale-adapters.yml`.

### test_stage6_release_gate_artifacts.py
- Release gate stage 6:
  - helper `collect_stage6_release_gate_issues()` отличает полный (PASS) и неполный (FAIL) набор evidence;
  - FAIL при строке acceptance `FAIL`;
  - wrapper-команды `test:stage6*` / `smoke:stage6*` в `package.json`;
  - workflow `.github/workflows/yearly-db-archive.yml` содержит jobs frontend/backend/build/production-smoke/windows-package/evidence-gate и обязательные smoke/evidence пути.

### test_yearly_archive_deploy_docs.py
- Операционный rollout stage 6:
  - unit: полнота `docs/yearly-db-archive-deploy.md` (окружение, миграция, rollback, ротация, smoke);
  - unit: README quick-check / storage и соответствие `package.json` scripts;
  - unit: installer layout `BD/` / `backup/` / `logs/` вне `_MEIPASS`;
  - E2E: rollout на копии legacy (preflight + migration + active-year);
  - E2E: post-rotation (backup, новый `active_year`, очистка сессии, ticket `#1`);
  - E2E: mixed legacy warning + archive check;
  - unit/runtime: `migration_target_exists` согласован с runbook.

### test_stage7_stub_contract.py
- Контрактные тесты stage-7 заглушек:
  - E2E: `GET /api/cameras/capability` (`available`/`build` по наличию cv2);
  - E2E: `POST /api/cameras/capture` → `noop=true` при `video_enabled=false` + непустой `capture_token`;
  - E2E: import `cameras`/`app` без OpenCV (subprocess с блокировкой `cv2`);
  - unit: `mask_url`, `PhotoStorage.resolve` path-traversal, идемпотентный `migrate_schema_stage_7`;
  - unit: partial POST без `app_cameras`/`app_ticket_photos` не чистит таблицы;
  - unit: defaults `video_enabled` / `camera_capture_timeout_sec` / `camera_jpeg_quality` в `read_config`.

### test_stage7_stub_skeleton.py
- Каркас stage-7 stub E2E/smoke:
  - live capability / capture noop / photos 404;
  - no-mock якорь «вес + capture stub» через `/api/database` + `/api/cameras/capture`;
  - unit: video defaults; smoke CLI `--help` / mode validation;
  - production-like smoke `capability` / `capture-noop` против `server/app.py`;
  - smoke `capture-http` / `capture-degrade` / `basic-import` / `full-import` (gate).

### test_stage7_database_cameras_roundtrip.py
- Roundtrip sync-ключей через `/api/database`:
  - пустые `app_cameras` / `app_ticket_photos`;
  - partial POST без этих ключей не очищает таблицы;
  - явные `[]` затирают ранее посеянные строки;
  - dump `app_ticket_photos` содержит только metadata/paths — без JPEG/base64 blobs (EC-02).

### test_stage7_release_gate.py
- Release gate stage 7 (якоря EC-10…EC-12 + packaging UC-07 + CI/deploy):
  - helper `collect_stage7_release_gate_issues()` — PASS на полном наборе тестов/скриптов;
  - FAIL при отсутствии файлов/якорей;
  - wrapper-команды `test:stage7` / `test:stage7-*` / `smoke:photo*` (http/degrade/basic-import/full-import) / `build:win:full*` в `package.json`;
  - `test:stage7-backend` включает RTSP timeout, release-gate, photo_storage, packaging tokens, deploy docs;
  - наличие `requirements-full.txt`, full spec/iss, `photo-capture-deploy.md`, workflow `photo-capture.yml` с basic/full smoke jobs.

### test_photo_capture_deploy_docs.py
- Deploy/docs/CI checks stage 7:
  - unit: полнота `docs/photo-capture-deploy.md` (migration v7, degrade, Photo/, rollback, secrets);
  - unit: `docs/api.md` содержит `/api/cameras/capture` и `/api/photos/`;
  - E2E: workflow `photo-capture.yml` с `import-smoke-basic` / `import-smoke-full`;
  - E2E: release artifacts (requirements-full, full spec/iss, acceptance EC PASS);
  - regression: stage5/stage6 workflows остаются отдельными файлами.

### test_stage7_packaging_tokens.py
- Dual packaging basic/full (UC-07):
  - unit: `requirements.txt` без opencv; `requirements-full.txt` с `opencv-python-headless`;
  - unit: full iss `OutputBaseFilename=WeighingSystem-Full-Setup` отличим от basic;
  - E2E token matrix: два spec + iss + `build.ps1 -Full` + Photo/ вне `_MEIPASS`;
  - E2E: `basic-import` smoke без cv2; `full-import` fail без opencv / pass при наличии;
  - E2E: installer Photo dir + runtime `PhotoStorage.ensure_photo_dirs` lazy write.

### test_stage7_migration.py
- Schema v7:
  - `user_version=7`, таблицы `cameras`/`ticket_photos`, обязательные индексы;
  - идемпотентность `migrate_schema_stage_7`;
  - `init_schema` доводит цепочку до stage 7.

### test_stage7_cameras_validation.py
- Валидация и preserve-non-null:
  - E2E: roundtrip 2 камер; POST 5 камер одного site → 400 без порчи таблицы;
  - unit: enabled без URL; bad role; pixel ROI; preserve etalon paths; preserve photo_* без `capture_token`;
  - unit: `capture_token` разрешает null overwrite stubs; partial POST без `app_cameras`.

### test_ticket_photos_replace.py
- Replace policy / F-08:
  - unit: `recompute_stubs` берёт последний success по `captured_at`;
  - E2E: success→failed сохраняет файл и stubs; success→success UNIQUE;
  - unit: ответ `ticket_photos` только для requested ticket_id;
  - unit: noop при `video_enabled=false`.

### test_cameras_capture_api.py
- `POST /api/cameras/capture` orchestration:
  - E2E: 2 HTTP cameras → 2 файла + stubs entry/exit;
  - E2E: mixed failed/unreachable → HTTP 200, вес не откатывается;
  - E2E: hung HTTP snapshot → `error_code=timeout`, вес/тикет целы;
  - E2E: `ticket_not_found` → 404 без файлов;
  - E2E: success затем failed через API сохраняет success;
  - unit: список камер из body игнорируется (SoT = SQLite);
  - unit: 0 enabled cameras → noop;
  - unit: `rotation_in_progress` → 409, Photo/ без новых файлов.

### test_photo_capture_orchestration.py
- Клиентский контракт flush→capture→upsert→flush (no-mock Flask):
  - TC-E2E-01: HTTP fixture + single save → файлы + stubs + preview в `app_ticket_photos`;
  - TC-E2E-02: dual gross затем tare → обе фазы;
  - TC-E2E-03: camera down → тикет completed + failed metadata;
  - TC-E2E-04: capture A + upsert flush не удаляет photos тикета B.

### test_stage7_rotation_cameras.py
- Year rotation stage-7 политики:
  - whitelist copy: `cameras` копируются, `ticket_photos` нет;
  - `validate_new_year_database` forbid nonzero `ticket_photos`;
  - E2E commit ротации: в новой БД есть cameras, `ticket_photos` count=0;
  - `count_stage6_tables` включает `cameras`.

### fixtures/minimal.jpg
- Синтетический 1×1 JPEG без персональных данных (HTTP snapshot / PhotoStorage фикстуры).

### test_cameras_capture_http.py
- HTTP snapshot (реальный локальный HTTPServer + `minimal.jpg`):
  - E2E: `POST /api/cameras/snapshot` → 200 + base64 JPEG;
  - E2E: закрытый порт → `503 camera_unreachable`, `/api/health` жив;
  - unit: HTTP path не импортирует `cv2`;
  - unit: `mask_url` для логов.

### test_cameras_capture_rtsp_timeout.py
- RTSP hard-timeout (мок «зависшего» `VideoCapture.open`):
  - unit EC-12: `error_code=timeout` за ≤ timeout+ε;
  - unit: RTSP без cv2 → `capability_unavailable`;
  - unit: пустые URL → `unreachable`.

### test_photo_storage.py
- Файловое хранилище Photo/:
  - unit: `write_ticket_photo` создаёт `Photo/YYYY/MM/DD/...`;
  - unit: path traversal reject;
  - unit: `write_etalon` overwrite, `safe_unlink` / `file_exists`, `ensure_photo_dirs`.

### test_video_settings_config.py
- Admin gate для video_* в POST `/api/config`:
  - weighing-only POST без session OK;
  - смена `video_enabled` → 401/403 без admin, 200 с admin;
  - повтор тех же video-значений без admin OK;
  - delete камеры → best-effort удаление `Photo/etalons/{id}/`.

### test_cameras_etalon_api.py
- `POST /api/cameras/etalon` (UC-02):
  - E2E: spare → `Photo/etalons/{id}/spare.jpg` + path; primary null;
  - E2E: ошибка камеры не перезаписывает прежний эталон;
  - E2E: stale client null не затирает etalon path при sync;
  - unit: path format; admin-only 403 для user.

### test_camera_logging.py
- Structured logging камер/фото (`camera_logging`):
  - unit: redaction URL (plaintext password отсутствует в `camera {...}`);
  - unit: payload содержит `ticket_id` / `capture_event` / `camera_id` / `status`;
  - unit: `video_enabled` / `photo_io` helpers;
  - E2E: capture degrade (timeout + unreachable) пишет лог без секретов;
  - E2E: смена `video_enabled` через POST `/api/config` → событие `video_enabled`/`changed`.

### test_photos_static_api.py
- `GET /api/photos/<path>` (UC-04):
  - JPEG без session; missing Origin OK; allowlist Origin OK;
  - чужой Origin → 403 `origin_not_allowed`;
  - path traversal → 400; missing file → 404.

## Зависимости
- `server/app.py` — Flask endpoints.
- `server/scale_api.py` и `server/scale_runtime.py` — runtime session lifecycle.
- `server/sqlite_store.py` и `server/persistence.py` — storage layer.
- `server/cameras.py`, `server/photo_storage.py`, `server/ticket_photos.py`, `server/camera_logging.py` — stage-7 capture/storage/observability.
- `server/stage6_logging.py` — structured operational logs stage 6.
- `scripts/smoke_photo_capture.py` — stage-7 CLI smoke.
