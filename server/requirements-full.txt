# Full Windows delivery: basic runtime + OpenCV (headless) for RTSP capture.
# Install on the full-build machine only; basic packaging must use requirements.txt.
-r requirements.txt
opencv-python-headless>=4.8.0,<5
